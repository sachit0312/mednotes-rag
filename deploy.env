## Deployment configuration for MedNotes RAG UI
# Static ngrok domain for API base (https URL implied if no scheme)
NGROK_DOMAIN=flea-whole-loosely.ngrok-free.app
# Optionally override with an explicit API URL
# API_BASE_URL=

# Vercel project configuration (avoid passing on every call)
# Set the exact project slug as seen in Vercel (e.g., mednotes_rag)
VERCEL_PROJECT=mednotes_rag
# If deploying under a team scope, optionally set:
# VERCEL_SCOPE=your-team-slug
